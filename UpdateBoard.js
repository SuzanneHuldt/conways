class UpdateBoard{

  updateBoard(board){

    if(this.isDead(cell) && //){
      else if(||//){

      }
    }
  }

  isDead(cell){
    return cell == 0
  }

  grabEnvironment(cell, board){
    var cX = cell[0];
    var cY = cell[1];
    var environment = [];
    environment.push()
  }

}
